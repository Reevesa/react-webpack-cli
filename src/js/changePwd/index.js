import React, { Component } from 'react';
// import './index.css'

import { Form, Icon, Input, Button, Checkbox } from 'antd';
const FormItem = Form.Item;

class ChangePwd extends Comment {
    constructor(props) {
        super(props);
    }


    render() {
        return (
            <div>
                changePwd
            </div>
        )
    }
}

export default ChangePwd